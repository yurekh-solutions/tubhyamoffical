feat: sync-first product load + dynamic reviews + themed footer badge

- ProductDetail: sync-first render (<2s target) with 8s AbortController
  timeout in api.ts to prevent Render free-tier cold-start hangs
- resolveProductId: case-insensitive SKU lookup, legacy alias map
  (fp-004→fp-015, fp-006→fp-017, tp-009→tp-013, fp-029→fp-014),
  MongoDB _id → curated product URL resolution
- Backend mapProduct: returns SKU (not MongoDB _id) as product.id
- useProducts: case-insensitive merge by SKU so uppercase backend IDs
  match lowercase static catalog SKUs
- ProductCard: deterministic per-product star rating + review count
- Footer: Yurekh Solutions pill badge with theme-aware dark-brown /
  bronze-gold styling; reduced top margin and inner padding
- Collections: tightened hero min-height, padding, and grid spacing
