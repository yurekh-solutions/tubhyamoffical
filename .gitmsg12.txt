security: hide API keys, proxy backend URL, fix domain, add rate limiting

- Remove hardcoded backend URL (onrender.com) from frontend JS bundle
- Add Vercel API proxy so all calls go through tubhyam.in/api/*
- Disable source maps in production build
- Delete twilio.ts and emailOtp.ts (dead code with exposed credentials)
- Create server-side /api/otp/send route (SMS credentials stay server-side)
- Strip internal fields from public blog API (campaignId, generationMode, etc.)
- Strip internal fields from products API (costPrice, currentStock, hsn, gstRate)
- Add express-rate-limit: 100 req/15min general, 20 req/15min sensitive routes
- Tighten CORS to only allow tubhyam.in and tubhyamoffical.vercel.app
- Fix domain mismatch: tubhyam.com -> tubhyam.in across all files
- Add security headers (X-Frame-Options, X-XSS-Protection, Referrer-Policy)
- Remove hardcoded admin secret fallback, use env var only
